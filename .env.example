# Monorepo: configure the API in server/.env (see server/.env.example).
# Client optional vars: client/.env.example
#
# Real auth (login, register, password reset tokens) needs a strong JWT_SECRET
# in server/.env or this file (server loads ../.env first). See server/.env.example.
